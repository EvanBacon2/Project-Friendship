using System.Collections;
using System.Collections.Generic;

//defines a property whose value can only be modified through requests
public class RequestableProperty<T> {
    T value;

    int priority;

    pub
}

//defines a set of properties on a model that can only be modified through requests
public class RequestableModel {
    
}
